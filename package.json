{
  "name": "resistorCompensation",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --platform=win32 --arch=x64 --app-version=0.1.0 --out=disrupt --ignore=disrupt --prune"
  },
  "dependencies": {
    "bootstrap": "^4.1.3",
    "electron": "^3.0.8",
    "electron-packager": "^12.2.0",
    "jquery": "^3.3.1",
    "mongodb": "^3.1.9"
  }
}
